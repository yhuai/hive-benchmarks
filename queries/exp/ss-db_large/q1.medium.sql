use ${DB};

EXPLAIN
SELECT SUM(v1),COUNT(*) FROM cycle
WHERE x BETWEEN 0 and 7500
AND   y BETWEEN 0 and 7500;

SELECT SUM(v1),COUNT(*) FROM cycle
WHERE x BETWEEN 0 and 7500
AND   y BETWEEN 0 and 7500;